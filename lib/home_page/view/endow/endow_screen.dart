import 'package:flutter/material.dart';

import 'package:get/get.dart';

class EndowScreen extends StatelessWidget {
  const EndowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('endow'),
      ),
    );
  }
}
